# Use Server Actions

## Reason
Mengurangi boilerplate pembuatan API route dan mempercepat pengembangan.

## Decision
Menggunakan Next.js Server Actions untuk semua mutasi data (create, update, delete, toggle status).
