# Deployment

Aplikasi di-deploy menggunakan **Vercel**.
- Build command menggunakan default dari Next.js (`npm run build`).
- Environment variables harus ditambahkan di Vercel Dashboard (Supabase credentials).
