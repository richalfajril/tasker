# Backend Stack

* Server Actions

Server Actions are used to handle data mutations directly, avoiding the need for dedicated API routes and external data fetching libraries like React Query or tRPC.
