# Server Actions

## Purpose
Memisahkan logika backend dalam Next.js Server Actions.

## Requirements
Action yang perlu dibuat:
- `createTask()`
- `updateTask()`
- `deleteTask()`
- `toggleTaskStatus()`

## Validation
Payload yang masuk ke dalam setiap server action harus divalidasi terlebih dahulu menggunakan `Zod`.
